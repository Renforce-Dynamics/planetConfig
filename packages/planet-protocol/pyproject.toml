[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
authors = [{name = "Renforce Dynamics"}]
maintainers = [{name = "Renforce Dynamics"}]
readme = "README.md"
license = {file = "LICENSE"}
name = "planet-protocol"
version = "0.1.0"
requires-python = ">=3.10"
description = "Operator, joint-target and localization wire contracts and clients"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["planet_protocol*"]

[tool.setuptools.package-data]
"*" = ["**/*.yaml", "**/*.json", "**/*.xml", "**/*.STL", "**/*.stl", "**/*.onnx", "**/*.npz", "**/*.hex", "**/*.md", "**/*.lock", "**/LICENSE"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.urls]
Repository = "https://github.com/Renforce-Dynamics/planetConfig"
Issues = "https://github.com/Renforce-Dynamics/planetConfig/issues"
Documentation = "https://github.com/Renforce-Dynamics/planetConfig#readme"
